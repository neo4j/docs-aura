= Data provision
:description: This page describes the data provision options in Neo4j Aura Document Intelligence

You can connect to either a *cloud data source* or stream *local files*.

The following file formats are supported:

* _PDF_
* _MD_
* _DOCX_
* _TXT_
* _EPUB_

== Cloud data sources

To connect to a cloud storage, navigate to the *Cloud data sources* tab and use the *Add data source* button to add a new source.
Once a source is added, it is listed in the table.

The following cloud storage options are supported:

* AWS S3
* Azure Blobs & Data Lake Storage
* Google Cloud Storage

Select the appropriate provider and enter your information.

[.shadow]
.Connection details
image::kgb-connect-gcp.jpg[width=600]

* *Name*: Give the connection a unique name.
This appears in the table of cloud data sources once added.

* *Bucket path*: Enter the URI for your bucket.
You can point to a specific file, add multiple paths, or use a wildcard (*).

* *Credentials*: Enter your HMAC access key and secret and then use *Confirm* to continue.

When the cloud source has been added successfully, you can opt to create a graph model right away via a dialog.


== Stream local files

To add local files to Document Intelligence, you need to navigate to the *Graph models* tab.
Use *Create graph model* to get started.

In the *data source panel* on the left, use the *Add data source* dropdown to select your local documents.
You can add up to 20 documents.

When you provide local files, only a reference to these are kept.
This is used to send the necessary commands to stream the data to your target database when you run the import.
Your local files themselves are *not* uploaded anywhere and therefore, if you reload the page before running the import, the files are no longer available to the page and you need to provide them again.
This is due to security features of your browser.


