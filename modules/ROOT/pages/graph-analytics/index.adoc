[[graph-analytics-aura]]
= Graph analytics in Aura
:description: This page describes how to run graph analytics in Aura.
:page-aliases: aurads/index.adoc
:gds-sessions-page: {neo4j-docs-base-uri}/graph-data-science/current/aura-graph-analytics/

Graph analytics uses graph algorithms to uncover patterns, detect anomalies, and predict behavior within connected data.
Typical use cases include:

* Fraud detection
* Supply chain optimization
* Recommendation engine development

Aura provides the cloud infrastructure to run complex graph algorithms at scale with the link:https://neo4j.com/docs/graph-data-science/current/[Neo4j Graph Data Science] (GDS) library.

[[aura-gds-serverless]]
== Aura Graph Analytics

Aura Graph Analytics is an on-demand ephemeral environment to run graph analytics on data stored anywhere, from AuraDB and self-managed Neo4j instances to non-Neo4j data sources.

With Aura Graph Analytics, you can create and delete _sessions_ on demand.
Sessions run as isolated Aura instances, with no memory or compute resources shared with your data store.

You can configure parameters like memory size and Time-To-Live (TTL) for each session according to your workload.
[[get-started-serverless]] See the xref:graph-analytics/aga.adoc[] page for more details.

[IMPORTANT]
====
If you plan to use Aura Graph Analytics from AuraDB, verify the following requirements:

* **AuraDB tier:** AuraDB Professional, Business Critical, or Virtual Dedicated Cloud
* **Neo4j version:** 5 or later

If you plan to use Aura Graph Analytics *outside* of an Aura environment (for example via the Python client), you need to create xref:api/authentication.adoc#_creating_credentials[Aura API credentials] too.
====

[[alternative-deployments]]
== Alternative deployment options

If the on-demand ephemeral architecture of Aura Graph Analytics is not suitable for your specific workload, Aura offers two alternative graph analytics solutions:

* [[aura-gds-plugin]] Enable the xref:graph-analytics/alternative-deployments.adoc#aura-gds-plugin[Graph Analytics plugin] on an existing AuraDB instance (Professional) for lightweight data exploration, where performance isolation is not critical.
* [[aura-ds]] Create an xref:graph-analytics/alternative-deployments.adoc#aura-ds[AuraDS instance] (Professional, Enterprise) when you need a long-running, persistent environment for collaboration or massive model hosting.

[[comparison]]
== Aura deployment comparison

[opts="header", cols="1s,1,1,1"]
|===
|
|Aura Graph Analytics (Recommended)
|Graph Analytics plugin +
(AuraDB Pro)
|AuraDS

|Primary use cases
|Data science, ETL, running algorithms, ephemeral workloads
|Lightweight exploration
|Persistent model serving, collaborative workspaces

|Compute architecture
|Serverless / On-demand (spin up, run, spin down)
|Shared instance (runs on the same resources as the database)
|Dedicated instance (always on, pausing possible)

|Billing
|Pay-as-you-go (per session minute)
|Included in the AuraDB cost
|Flat hourly rate (per instance size)

|Data sources
|AuraDB, self-managed Neo4j, Pandas DataFrames
|AuraDB (integrated storage)
|AuraDS (integrated storage), Pandas DataFrames

|Resources for analytics
|Full isolation (no impact on database performance)
|Shared (can slow down transactional database)
|Full isolation or shared footnote:[If the source transactional database is used on the same AuraDS instance, graph analytics can impact its performance.]

|Machine learning
|Train and predict (session-scoped)
|Limited
|Train, store and serve models

|===

.Technical comparison
[opts="header", cols="1s,1,1,1,1"]
|===
|
|Graph Data Science +
on-premisesfootnote:[Assuming use of the link:{neo4j-docs-base-uri}/graph-data-science/current/installation/installation-enterprise-edition/[GDS Enterprise Edition].]
|Aura Graph Analytics +
(Recommended)
|Graph Analytics plugin +
(AuraDB Pro)
|AuraDS

|Data sources
|Same server
|AuraDB (same tier); self-managed Neo4j DBMS; custom
|Same server
|Same server; custom

|Resources for analytics
|Shared with database +
(user-configured)
|Dedicated
|Shared with database +
(optimized for database)
|Shared with database +
(optimized for analytics)

|Maximum memory
|No upper limit +
(contract-dependent)
|Up to 128 GB (Pro, BC) or 512 GB (VDC)
|Up to 128 GB
|Up to 384 GB

|Number of concurrent GDS sessions
|-
|Up to 10 (Pro, BC) or 100 (VDC)
|-
|-

|Restart behavior
4+|

>|_Downtime_
|Short
|No
|No
|Short

>|_Projected graphs_
|Must be backed up and restored manually
|Unaffected
|Not retained
|Restored automaticallyfootnote:createdafter[Graphs and models created or updated _after_ the instance update/upgrade process has started are *not guaranteed* to be restored upon restart.]

>|_Trained models_
|Must be backed up and restored manually
|Unaffected
|Not retained
|Restored automaticallyfootnote:createdafter[]

|===
